# Server Configuration
PORT=3000
HOST=0.0.0.0

# JWT Configuration
# REQUIRED in production. Must be at least 32 characters and must not be a known
# default/placeholder value. Generate a strong secret with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRY=24h

# Separate secret used to sign refresh tokens (EnterpriseAuth).
# Apply the same strength requirements as JWT_SECRET above.
REFRESH_TOKEN_SECRET=your-refresh-secret-here-change-in-production

# Separate secret used to sign refresh tokens (AuthManager).
# Apply the same strength requirements as JWT_SECRET above.
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here-change-in-production
JWT_REFRESH_EXPIRY=7d

# Database Configuration
MONGODB_URL=mongodb://localhost:27017/easyjs
MYSQL_URL=mysql://root:password@localhost:3306/easyjs
POSTGRES_URL=postgres://postgres:password@localhost:5432/easyjs
SQLITE_URL=sqlite://./data/easyjs.sqlite
LIBSQL_URL=
LIBSQL_AUTH_TOKEN=
MSSQL_URL=sqlserver://sa:password@localhost:1433/easyjs?trustServerCertificate=true
NEO4J_URL=neo4j://neo4j:password@localhost:7687
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_API_KEY=
ELASTICSEARCH_INDEX=live_users
CASSANDRA_CONTACT_POINTS=127.0.0.1
CASSANDRA_KEYSPACE=easyjs
CASSANDRA_LOCAL_DATACENTER=datacenter1
CASSANDRA_USERNAME=
CASSANDRA_PASSWORD=
CASSANDRA_SSL=false
CASSANDRA_SECURE_CONNECT_BUNDLE=
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_TABLE=live_users
FIREBASE_PROJECT_ID=
FIREBASE_DATABASE_URL=
FIREBASE_CREDENTIALS_JSON=
FIREBASE_CREDENTIALS_BASE64=
FIREBASE_CREDENTIALS_FILE=
DYNAMODB_ENDPOINT=
DYNAMODB_TABLE=live_users

# Redis Configuration (Optional)
REDIS_URL=redis://localhost:6379

# Environment
NODE_ENV=development
DEBUG=false

# CORS Configuration
# Use "*" only for public APIs that do not use cookies or credentialed requests.
# For credentialed CORS, set an explicit comma-separated allowlist.
CORS_ORIGIN=*
CORS_CREDENTIALS=false

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Observability
LOG_LEVEL=info
SENTRY_DSN=
PROMETHEUS_ENABLED=true

# AI Providers
AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.1
OPENAI_EMBEDDING_MODEL=text-embedding-3-large
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-pro
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# Security / Compliance
FIELD_ENCRYPTION_KEY=replace-with-32-byte-secret
API_KEY_SALT=replace-with-api-key-salt

# Cloud Storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=
GCP_PROJECT_ID=
GCP_KEY_FILE=
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=

# Payments
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email Providers
SENDGRID_API_KEY=
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
